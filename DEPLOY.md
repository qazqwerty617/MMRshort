# 🚀 Инструкция по деплою на сервер (VPS)

Чтобы бот работал 24/7 (даже когда вы закрываете компьютер), его нужно запустить на сервере.

## 1. Подготовка (на сервере)

Зайдите на свой сервер через терминал (Putty или cmd).

Перейдите в папку с ботом:

```bash
cd /root/MMR
```

*(Или другой путь, где лежит бот)*

Получите последние обновления (конфиг, новые библиотеки):

```bash
git pull
```

Обновите библиотеки (мы добавили ML компоненты):

```bash
source .venv/bin/activate
pip install -r requirements.txt
```

---

## 2. Запуск 24/7 (Вариант "Простой" - Screen)

Если вы просто хотите запустить и забыть, используйте `screen`.

1. **Создайте новую сессию:**

   ```bash
   screen -S mmr
   ```

2. **Запустите бота:**

   ```bash
   python bot_rest.py
   ```

3. **Выйдите из сессии (оставив бота работать):**
   Нажмите `Ctrl+A`, затем `D`.

   *(Бот продолжит работать в фоне)*

**Чтобы вернуться к боту:**

```bash
screen -r mmr
```

**Чтобы остановить:**
Нажмите `Ctrl+C` внутри сессии.

---

## 3. Запуск 24/7 (Вариант "Профи" - Systemd)

Если вы хотите, чтобы бот сам запускался после перезагрузки сервера.

1. **Скопируйте файл сервиса:**

   ```bash
   cp mmr_bot.service /etc/systemd/system/
   ```

2. **Обновите конфигурацию systemd:**

   ```bash
   systemctl daemon-reload
   ```

3. **Включите автозапуск:**

   ```bash
   systemctl enable mmr_bot
   ```

4. **Запустите бота:**

   ```bash
   systemctl start mmr_bot
   ```

**Полезные команды:**

- Статус: `systemctl status mmr_bot`
- Логи: `journalctl -u mmr_bot -f`
- Стоп: `systemctl stop mmr_bot`
- Рестарт: `systemctl restart mmr_bot`

---

## ⚠️ Важно

Убедитесь, что в файле `mmr_bot.service` (если используете вариант 3) указан правильный путь к папке и python!
По умолчанию там стоит: `/root/MMR/`
Если у вас другая папка, отредактируйте файл `mmr_bot.service` перед копированием:

```bash
nano mmr_bot.service
```
