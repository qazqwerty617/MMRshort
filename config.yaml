# MEXC Futures Pump Detector Bot - Конфигурация

# ============ НАСТРОЙКИ TELEGRAM ============
telegram:
  bot_token: "8315354749:AAFcU0LzGUkMCe2Qodl2w9DpIuP1oGZv-vU"
  chat_id: "-1003582014728"  # ID группы
  topic_id: 2  # ID темы (треда) в группе
  message_format: "markdown"

# ============ НАСТРОЙКИ MEXC API ============
mexc:
  api_key: "mx0vglFgBe7a9UgIMh"
  api_secret: "8ce08f2ddf1844d5b68186c1c4d62d39"
  ws_endpoint: "wss://contract.mexc.com/edge"
  rest_endpoint: "https://contract.mexc.com"
  ping_interval: 15

# ============ МОНИТОРИНГ ============
monitoring:
  # Максимальное количество одновременно отслеживаемых пар
  # 10000 = мониторим ВСЕ пары без ограничений
  max_concurrent_pairs: 10000
  
  # Паттерны для игнорирования (regex) - ОТКЛЮЧЕНО, мониторим ВСЕ пары
  ignore_patterns: []

# ============ ПАРАМЕТРЫ ОБНАРУЖЕНИЯ ПАМПА ============
pump_detection:
  # 🚀 FAST PUMP: Быстрые ювелирные пампы
  fast_pump:
    min_increase_pct: 10.0       # Минимум +10%
    max_timeframe_minutes: 5.0   # За ≤5 минут
  
  # ⚡ ELITE PUMP: Сильные пампы
  elite_pump:
    min_increase_pct: 20.0       # Минимум +20%
    max_timeframe_minutes: 20.0  # За ≤20 минут
  
  # 📢 Повторный сигнал: только если рост +10% от прошлого пика
  repeat_signal_threshold: 10.0
  
  # Общие параметры
  max_price_increase_pct: 300.0
  min_volume_spike: 1.2
  min_volume_usd: 10000          # Объём оставляем низкий ($10k)

# ============ ПАРАМЕТРЫ АНАЛИЗА ВХОДА В ШОРТ ============
short_entry:
  min_volume_drop_pct: 20.0  # Вернули из бэкапа
  rsi_period: 14
  rsi_overbought: 70
  macd_fast: 12
  macd_slow: 26
  macd_signal: 9
  min_divergence_strength: "средняя"
  min_sell_wall_pct: 5.0
  orderbook_depth: 20

# ============ ГЕНЕРАЦИЯ СИГНАЛОВ ============
signal:
  min_quality_score: 7.0  # 🔥 ELITE MODE: Только качественные сигналы!
  min_risk_reward: 2.0    # Минимум 1:2 соотношение риск/профит
  stop_loss_atr_multiplier: 1.5
  take_profit_levels:
    - 3.0
    - 6.0
    - 10.0

# ============ МАШИННОЕ ОБУЧЕНИЕ ============
learning:
  enabled: true
  min_signals_for_learning: 10
  tracking_periods:
    - 5
    - 15
    - 60
    - 240
  learning_rate: 0.1
  
  # ОПТИМИЗИРОВАННЫЕ веса с учётом новых факторов
  initial_weights:
    divergence: 0.35
    volume_drop: 0.30
    orderbook: 0.20
    rsi_level: 0.15

# ============ БАЗА ДАННЫХ ============
database:
  path: "data/pump_detector.db"
  auto_cleanup_days: 90

# ============ ЛОГИРОВАНИЕ ============
logging:
  level: "INFO"
  file: "logs/bot.log"
  max_file_size_mb: 10
  backup_count: 5
